let auth        = require('./auth')
let components  = require('./components')
let projects    = require('./projects')
let users       = require('./users')

module.exports = { auth, components, projects, users }